import React from 'react'
import Typography from "@mui/material/Typography";
import Layout from "../layout/Layout";

const Superviseur = () => {
    
    return (
        <Layout>
            <Typography variant="h1">Superviseur</Typography>
        </Layout>
    )            
}

export default Superviseur
